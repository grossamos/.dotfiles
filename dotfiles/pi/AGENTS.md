You are in a sandbox and only have the files available in your current working directory.
Among standard tooling, the following tools additionally are available to you: `xh` (to fetch websites) `kagi-cli` (for websearch).
ALLWAYS prefer `xh` over curl.
If you need a specific tool ask the user to give you access.
